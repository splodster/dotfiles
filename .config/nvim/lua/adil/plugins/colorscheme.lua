return {
    "sainnhe/gruvbox-material",
    priority = 1000,
    opts = {},
    config = function()

        -- setup must be called before loading
        vim.cmd("colorscheme gruvbox-material")
    end
}

